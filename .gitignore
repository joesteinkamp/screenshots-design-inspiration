# General Mac OS
.DS_Store
.AppleDouble
.LSOverride


# Windows thumbnail cache files
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db

# Windows Dump file
*.stackdump

# Jekyll
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata

# Ruby
vendor/
.bundle/
Gemfile.lock
node_modules

# MCP Server
mcp-server/dist/
.DS_Store
Web/.DS_Store

# Local tagger model weights (downloaded on demand, ~6GB).
# No trailing slash: this may be a real dir or a symlink to a shared model store.
scripts/.models
# Native llama-server bookkeeping from start-local-tagger.sh
scripts/.tagger.pid
scripts/.tagger.log

# Backfill logs
logs/

# Build warnings report (regenerated each jekyll build by _plugins/safe_frontmatter.rb)
_build_warnings.json

# Local playwright-cli preview artifacts
.playwright-cli/
